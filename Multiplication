int multiplyNumbers(int m, int n) {
    int result = 0;
    if(n==1)
    {
        return m;
    }
    else if (m==1)
    {
        return n;
    }
    else {
    result += m + multiplyNumbers(m,n-1);
    }
    return result;
}
